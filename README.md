# Prompt2Plot Interactive Visualization

Static interactive visualization for the Prompt2Plot portfolio.

The app is self-contained except for `Student.csv`, which must stay next to `index.html` when deployed.
